# Attention geometry — public pack

Technical note: [attention geometry](../attention-geometry.html)
(route `/technical-reports/attention-geometry`).

## One-file check (no install)

```bash
python3 verify_ml_beta.py
```

Decides rational-kernel PR↓ on the frozen fixture (exact `fractions`), checks
planted mutants, and verifies the fixture sha256 pin shared by the three
certificate JSON files in `certs/`.

Softmax H/PR sound enclosure uses interval `exp`/`log` in the exported
`eqcert` transcendental module (Node batteries in the source monorepo).

## Scope

We certify the **decision / enclosure on this fixture**, not the classical
softmax concentration theorem (Mattei–Loureiro arXiv:2602.14862 Prop. 2;
Dabah–Tirer, ICML 2025).
